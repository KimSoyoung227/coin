# 작업 일지

## 2026-09-11 — 계산 모듈
- 요구사항 검토: 기존 `coin` 디렉터리를 사용하고 표준 `src/coin`, `tests` 구조로 구성.
- DB 불필요: 로그인/공유/영구 저장 없이 브라우저 sessionStorage로 입력을 보관할 예정. 최초 저장 후 1시간 만료 정책 적용 예정.
- 수익률, 물타기/불타기 평균 단가, 투자금액→수량 변환 구현. 입력은 float 변환, 유한성/범위 검증.
- 수수료 정책: 입력한 비율을 매수와 매도 각각 적용. 수익률 분모는 수수료 제외 매수 원금. 평균 단가는 요구사항 공식대로 수수료 제외.
- 외부 패키지 설치 없음. .gitignore에 환경/비밀정보/IDE 파일 추가.
- 검증: `PYTHONPATH=src .venv/bin/python -m unittest discover -s tests -v` — 5개 테스트 통과.
- 계산 모듈 검토 후 Python 웹 모듈, 반응형 화면 및 세션 만료 모듈 순서로 진행 예정.

## 2026-09-11 — Python 웹 모듈
- 사용자 계산 모듈 승인 후 Flask 앱 팩토리 및 `POST /api/calculate` 구현.
- 수익률/물타기/불타기 연결, 수량 또는 투자금액 입력 및 추가 매수금액 환산 지원.
- 잘못된 JSON/자료형/필수 항목 누락/중복 입력을 검증하고 요청 크기 16 KiB 제한.
- 서버 저장 및 쿠키 없음. 응답 캐싱 금지와 기본 보안 헤더 적용.
- 필수 직접 의존성 Flask만 추가, 설치 버전 3.1.3. pyproject.toml에 표준 패키지 설정.
- 검증: 계산 5개 + API 4개, 총 9개 테스트 통과.
- 웹 모듈 검토 대기. 반응형 화면과 브라우저 세션 저장/1시간 만료는 아직 미구현.

## 2026-09-11 — 화면 및 세션 모듈
- 사용자 승인 후 한국어 원페이지, 수익률/물타기/불타기 탭, 입력 설명, 확인 버튼, 결과 및 저작권 표시 구현.
- 큰 화면의 양쪽 광고 예약 공간, 모바일 단일 열 입력, 키보드 탭 이동 및 오류/결과 접근성 적용.
- 수량/투자금액 선택 및 추가 매수금액 선택을 API에 연결.
- sessionStorage에 탭별 초안 보관, 새로고침 복원, 최초 저장부터 고정 1시간 만료. 만료/초기화 시 화면과 저장값을 함께 삭제.
- 백그라운드 복귀 시 만료 재확인, 저장소 차단 대응, 입력 변경/만료 시 진행 중 요청 취소 및 오래된 응답 폐기.
- Python 통합 테스트 및 Node.js 세션 테스트로 검증. 브라우저 실화면 테스트는 수행하지 않음.
- 추가 Python 패키지 없음. 실행 및 코드 인덱스를 README에 갱신.

## 2026-09-11 — 로컬 403 진단
- 5000 포트에서 macOS ControlCenter와 Python 프로세스가 함께 수신 중인 것을 확인.
- `localhost:5000` 응답은 `Server: AirTunes/760.20.1`, HTTP 403으로 확인. 계산기 권한 문제가 아닌 다른 서비스 응답.
- README에 IPv4 주소 접속 및 대체 포트 5050 사용 방법 추가.

## 2026-09-11 — 탭 전환 오류 수정
- 폼 내부 초기화 버튼의 `id="reset"`이 브라우저의 `form.reset()` 메서드를 가려 탭 전환이 중단되는 원인 확인.
- 버튼 id를 `clear-inputs`로 변경하고 이벤트 연결을 함께 수정. 탭 전환, 초기화 및 세션 복원에서 같은 충돌 제거.
- 폼 내 id/name과 내장 메서드 충돌을 검출하는 회귀 테스트 추가.
- Python 테스트 11개 및 세션 테스트 통과. 연결 가능한 브라우저가 없어 실제 클릭 검증은 수행하지 못함.

## 2026-09-11 — 사용자 검토 완료 및 실행 정리
- 사용자가 전체 화면과 기능 확인 완료를 전달함.
- `python -m coin` 실행 진입점 추가. 기본 주소 127.0.0.1:5050으로 AirPlay 포트 충돌 방지, 디버그 기본 비활성화.
- 검증된 실행 의존성 버전을 requirements.txt에 기록하고 PyCharm 실행 설정 문서화.
- 최종 검증: Python 테스트 11개, 세션 테스트, 실행 도움말, git diff --check 통과.
- wheel 패키지 빌드 성공 및 HTML/CSS/JavaScript/실행 진입점 포함 확인.
- 요구사항의 로컬 개발 구현 완료. GitHub 업로드와 외부 서버 배포는 수행하지 않음.

## 2026-09-11 — GitHub 업로드 준비
- 사용자 요청으로 origin 저장소(KimSoyoung227/coin)의 main 브랜치에 구현 전체 업로드 진행.
- Python 테스트 11개, 세션 테스트 및 공백 오류 검사 재확인 통과.
- 가상환경, IDE 설정, 환경변수 파일 및 빌드 산출물은 .gitignore로 제외.
- 운영 포트 50500 선호는 확인했으며, 실제 운영 서버 설정과 외부 배포는 별도 작업으로 남아 있음.

## 2026-09-15 - Google Ad 코드 추가
- create_app() 에서 CSP 설정 추가.

## 2026-09-15 — 양쪽 광고 영역 제거
- 화면의 왼쪽·오른쪽 광고 예약 공간과 관련 CSS를 삭제.
- 전체 화면 크기에서 계산기를 최대 너비 800px의 단일 열로 중앙 정렬하고, 광고 숨김 전용 1100px 분기 제거.
- 540px 이하 화면의 여백, 입력 및 결과의 단일 열 전환, 버튼 세로 배치 유지.
- README의 스타일 파일 설명을 현재 레이아웃에 맞게 수정. Google AdSense 계정 메타 태그와 로딩 스크립트는 유지.
- 검증: Python 테스트 11개 및 git diff --check 통과. 실제 브라우저 화면 검증은 수행하지 않음.

## 2026-09-15 — 실제 브라우저 검증 시도
- 로컬 개발 서버를 127.0.0.1:5051에서 실행하고 브라우저 연결을 시도.
- 연결 가능한 브라우저가 없으며, 실행 중인 Google Chrome에 대한 컴퓨터 제어 접근도 승인되지 않아 화면 검증을 진행하지 못함.
- 데스크톱·태블릿·모바일 화면의 중앙 정렬, 가로 넘침, 입력·결과 배치에 대한 실제 브라우저 검증은 미완료.

## 2026-09-18 — 4개 언어 지원 (v0.1.0)
- 상단 오른쪽에 영국·스페인·중국·한국 Unicode 국기 이모지를 사용한 원형 언어 버튼 추가. 별도 저작권 이미지 파일과 외부 요청 없음.
- 한국어·영어·스페인어·중국어 간 제목, 탭, 입력, 안내, 오류, 결과 및 접근성 문구 전체 전환 구현.
- 선택 언어를 기존 탭 세션에 함께 저장하고, 언어별 숫자 형식과 결과 단위를 적용.
- 서버나 DB 저장은 불필요하여 추가하지 않음. 외부 패키지 추가 없음.
- 번역 사전의 언어별 키 완전성과 기본 언어 처리 테스트 추가.
- 검증: Python 11개 테스트, 세션 테스트, 4개 언어 사전 테스트, JavaScript 문법 검사 및 `git diff --check` 통과. 브라우저 제어 환경이 없어 실제 클릭 검증은 수행하지 못함.

## 2026-09-18 — 4개 통화 단위 지원 (v0.2.0)
- 기존 KRW 안내 문구를 KRW·GBP·EUR·CNY 선택 상자로 변경.
- 통화 변경 시 매수·매도·추가 매수 입력과 금액형 입력, 계산 결과의 단위를 즉시 변경하도록 구현.
- 선택 통화를 기존 브라우저 탭 세션에 함께 저장하고 새로고침 시 복원.
- 환율 변환 요구가 없어 입력 숫자를 선택 통화 기준으로 계산하며 외부 API, DB 및 추가 패키지는 사용하지 않음.
- 통화 선택 항목과 입력 단위 연결에 대한 웹 테스트 추가.
- 개발 버전을 `0.2.0`으로 갱신.

## 2026-09-18 — 미국 언어·통화 지원 (v0.3.0)
- 상단 언어 선택에 미국 국기 버튼과 미국 영어 로케일을 추가.
- 통화 선택에 미국 달러 USD($)를 추가하고 입력 및 결과 단위, 세션 복원에 연결.
- 미국 영어는 영어 문구를 공유하며 숫자는 미국식 형식으로 표시.
- 미국 언어 버튼과 USD 선택 항목 회귀 테스트 추가, 개발 버전을 `0.3.0`으로 갱신.

## 2026-09-18 — 언어 아이콘 정리 (v0.3.1)
- 언어 아이콘을 한국·미국·중국·스페인 순서로 변경.
- 미국과 문구가 동일한 영국 아이콘 및 영국 로케일을 제거하고, 기존 영국 영어 세션은 미국 영어로 자동 이전.
- 아이콘 순서와 영국 제거 회귀 테스트 추가, 개발 버전을 `0.3.1`로 갱신.

## 2026-09-18 — 평균 단가 탭 통합 (v0.4.0)
- 불타기 탭을 제거하고 물타기 탭을 방향 제한 없는 `평균 단가 계산` 탭으로 변경.
- 추가 매수 단가가 기존 단가보다 낮거나 높아도 동일한 평균 단가 API에서 계산하도록 통합.
- 한국어·미국 영어·중국어·스페인어의 탭, 설명 및 메타 설명을 새 기능에 맞게 변경.
- 기존 물타기·불타기 브라우저 초안을 평균 단가 계산 탭으로 자동 이전.
- README, API 및 탭 회귀 테스트를 갱신하고 개발 버전을 `0.4.0`으로 변경. DB와 추가 패키지는 사용하지 않음.

## 2026-09-18 — 환율·시세 조회 (v0.5.0)
- PC 계산기 오른쪽, 1100px 이하 화면에서는 계산기 하단에 배치되는 간소한 시세 표 추가.
- ExchangeRate.fun에서 KRW·USD·CNY·JPY·EUR·GBP 환율을, Gold API에서 금·은·구리·BTC 원화 가격을 조회.
- 금·은은 트로이온스, 구리는 파운드 기반 공급가를 g 단가로 환산하여 표시.
- 서버 메모리에 BTC 1분, 금·은·구리 5분, 통화 1시간 캐시 적용. DB와 API 키 및 추가 패키지는 불필요.
- 그룹별 조회 완료 시각, 이전 캐시 여부, 부분 오류와 API 출처를 사용자 화면에 표시.
- 외부 응답 크기·형식·숫자 검증과 호출 시간 제한을 적용하고 단위 환산·캐시·부분 실패·웹 API 테스트 추가.
- 최초 로딩 시간을 줄이기 위해 세 시세 그룹과 금속 3종을 병렬 조회하고 그룹별 잠금으로 중복 요청을 방지.
- 실제 공개 API 연동 검증에서 통화 6종, 금속 3종, BTC가 오류 없이 원화 기준으로 반환됨을 확인.
- 공식 문서 확인: Gold API 무료 가격 엔드포인트와 ExchangeRate.fun 무료 시간별 환율 엔드포인트. 개발 버전 `0.5.0`.

## 2026-09-18 — 국가별 환율 비교 (v0.5.1)
- 시세 카드에 `기준 통화 → 대상 통화` 선택 상자와 실시간 교차 환율 결과 추가.
- 언어 지원 국가의 KRW·USD·CNY·EUR를 양쪽에서 선택할 수 있으며 서버 캐시 환율을 재사용해 추가 외부 호출 없이 계산.
- 비교 UI와 접근성 문구를 4개 언어로 제공하고 개발 버전을 `0.5.1`로 변경.

## 2026-09-18 — 시세 표 기준 통화 전환 (v0.5.2)
- `기준 통화 → 대상 통화` 비교 UI를 제거하고 KRW·USD·CNY·EUR 단일 기준 통화 선택 상자로 변경.
- 기준 통화 변경 시 통화 6종, 금·은·구리 및 BTC 표 전체 가격을 해당 통화로 즉시 환산.
- 통화 자산명에 ISO 코드를 함께 표시하고 가격 열과 g·BTC 단위에 선택 통화 코드를 반영.
- 기존 서버 캐시 값을 활용하므로 추가 외부 API 호출은 없으며 개발 버전을 `0.5.2`로 변경.

## 2026-09-18 — 일본어·엔화 지원 (v0.6.0)
- 언어 아이콘에 일본 국기와 일본어 전체 번역팩을 추가하고 일본식 숫자·날짜 형식을 적용.
- 계산기 화폐 종류와 환율·시세 표 기준 통화에 국제 표준 코드 JPY(일본 엔)를 추가.
- 기존 ExchangeRate.fun 응답의 JPY 환율을 재사용하므로 외부 호출 및 패키지는 추가하지 않음.
- 일본어 번역 키 완전성, 국기 및 두 JPY 선택 항목 회귀 테스트를 추가하고 개발 버전을 `0.6.0`으로 변경.

## 2026-09-18 — 시세 표 가로 너비 조정 (v0.6.1)
- PC 시세 사이드바 너비를 300px에서 360px로 확대하고 가로 스크롤 속성을 제거.
- 긴 다국어 자산명은 셀 안에서 줄바꿈하고 가격은 한 줄로 유지하도록 조정.
- 1100px 이하에서는 기존처럼 하단 전체 너비를 사용하며 개발 버전을 `0.6.1`로 변경.

## 2026-09-19 — 매도 단가 비율 버튼
- 수익률 계산 탭의 매도 단가 입력창 위에 5%·10%·20% 버튼 추가. 매수 단가에 해당 상승률을 적용한 가격을 입력하며 수수료는 별도로 계산.
- 기존 입력 이벤트를 통해 결과 무효화 및 세션 저장을 적용하고, 잘못된 매수 단가와 세션 만료를 확인.
- 버튼을 작게 조정하고 안내 문구를 삭제. 매수·매도 입력창의 정렬과 글자 스타일을 통일.
- 검증: Python 테스트 21개, 기존 번역·세션 테스트, JavaScript 문법 검사 및 버튼 핸들러 실행 검증 통과. 브라우저 실화면 검증은 수행하지 않음.

## 2026-09-19 — 개발 규칙 보완 (v0.7.0)
- `AGENTS.md`에 모듈별 검토, 최소 답변·토큰, 경로 안내, 주석, 보안, 작업일지 및 커밋 버전 규칙 명시.
- 매도 단가 버튼 기능 추가에 맞춰 개발 버전을 `0.6.1`에서 `0.7.0`으로 갱신. 기존 커밋 메시지는 변경하지 않음.
- 매도 버튼 처리, 세션 함수 및 Python 함수의 누락된 설명 주석 보완.
- `.gitignore`의 환경변수·개인키·비밀정보 디렉터리 제외 규칙 확인.
- 사용자가 로컬에서 UI 수정 확인을 완료함. 작업일지의 버튼 위치와 안내 문구 설명을 최종 상태로 갱신.
- 검증: Python 테스트 21개, JavaScript 세션·번역 테스트 및 변경 파일 공백 검사 통과.

## 2026-09-19 — 매도 단가 버튼 문구 수정 (v0.7.1)
- 버튼 문구를 요청대로 `+5%`, `+10%`, `20%`로 수정. 계산 동작은 유지.
- 개발 버전을 `0.7.1`로 갱신하고 버튼 문구 및 변경 파일 공백 검사 완료.

## 2026-09-19 — 언어별 페이지 및 다국어 SEO (v0.8.0)
- 실제 요구사항 파일 `codex/20260919_코인 수익률 계산기 사이트_언어별 페이지 분리.md` 기준으로 `/ko/`, `/en/`, `/ja/`, `/zh/`, `/es/` 추가.
- 사용자 확인에 따라 루트는 접속 IP가 아닌 브라우저 선호 언어로 302 이동. 미지원·누락 시 영어를 사용하고 응답에 `Vary: Accept-Language` 적용.
- 번역 사전을 `translations.json`으로 통합하고 title·meta description·h1·설명·레이블·접근성 문구를 서버 렌더링. 번역 모듈 검토 후 사용자에게 계속 진행 확인받음.
- 국기 아이콘을 hreflang이 있는 실제 링크로 변경. URL 언어를 이전 세션 언어보다 우선하며 입력값·탭·통화 복원은 유지.
- 고정 공개 도메인의 self-canonical, 상호 hreflang과 x-default, XML 사이트맵, robots.txt 추가. 패키지 데이터에 번역 JSON과 XML 템플릿 포함.
- 개발 버전 `0.8.0`, README 코드 인덱스와 동작 설명 갱신. DB·추가 의존성·비밀정보 없음.
- 검증: Python 테스트 26개, JavaScript 번역·세션·5개 URL 언어 및 입력 복원 테스트 통과. 브라우저 연결 불가로 실제 화면 검증은 미수행. 임시 로컬 검증 서버 종료.
