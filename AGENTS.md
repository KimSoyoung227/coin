# 프로젝트 작업 지침

- 각 모듈 개발과 검증을 마치면 사용자에게 검토 확인을 요청한다. 이미 확인한 내용은 재요청하지 않는다.
- 불필요한 탐색과 반복을 줄여 토큰 사용을 최소화한다.
- 답변은 간결하게 작성하고 코드 본문 대신 코드 인덱스 또는 파일·폴더 경로를 안내한다.
- 개발·수정하는 함수와 모듈에는 역할을 설명하는 주석을 작성한다.
- 비밀정보와 로컬 보안 설정은 커밋하지 않도록 `.gitignore`에 등록한다. 실제 비밀값은 출력하지 않는다.
- 작업 내용과 검증 결과를 `WORK_LOG.md` 최하단에 날짜순으로 기록한다.
- 개발 버전은 `Major.Minor.Patch` 형식으로 관리하고 정식 출시 전 Major는 0으로 유지한다.
- 기능 추가는 Minor, 수정은 Patch를 올리고 `pyproject.toml`과 작업일지에 반영한다.
